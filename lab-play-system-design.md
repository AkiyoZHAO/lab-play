# lab / aki 个人行为游戏化系统 — 规划书

> 定位：对抗职业倦怠期"耗竭+无意义感"的低摩擦系统，用游戏化机制把散落的"摸鱼"行为收敛成可持续、可回顾的个人系统。核心原则：**极简、低摩擦、单向记账、不靠意志力**。

---

## 一、系统总览

三个子系统，职责分离，共享同一份底层数据：

| 子系统 | 定位 | 与数据库的关系 |
|---|---|---|
| 抽卡系统（随机触发器） | 对抗决策疲劳，随机推荐"有点意思但不想设为每日任务"的活动 | **入口**：完成后写入一条事件 |
| 代币账本（努力证据库） | 唯一数据源（single source of truth），记录"发生了什么" | **核心**：一张事件表，其他系统都是它的读/写视图 |
| 收集图鉴系统（成就出口） | 把累积的事件转化为可感知的"没有原地踏步"的证据 | **出口**：基于规则扫描事件表，不单独存状态 |

**关键设计判断**：
- 账本不做"双向货币"（赚币-花币-兑换定价），改为**单向流水账**，只记录发生了什么，不设手动定价的消费出口——手动定价是最容易半途而废的环节。
- 图鉴系统的终点是"证据"而非"奖励"：对于已经不需要用外部奖励激励自己的成年人，"我这段时间确实没有原地踏步"这份可视化证据，比兑换机制更耐用。
- 三个已有的零摩擦外部打卡（多邻国、Switch Fitness Boxing、微信读书）**暂不纳入自动同步**，除非能确认三者有可自动化拉取的数据接口（API/导出），否则手动录入会违背极简原则，宁可暂时空着这块。

---

## 二、抽卡系统（Draw System）

### 2.1 核心机制
- 已有雏形：随机抽取一个"觉得还可以但不想设为每日任务"的活动（如唱歌、弹钢琴、跳舞，一次 30min），针对"下班后瘫沙发想刷手机"的场景。
- 升级方向：**按能量/时长分档抽卡**，而不是所有活动同权重随机。

### 2.2 分档建议（可自行调整命名和阈值）

| 档位 | 时长 | 示例 | 适用场景 |
|---|---|---|---|
| R（低消耗） | ~5min | 唱一首歌、听一集播客片段 | 电量很低，只想有点刺激但不想动脑 |
| SR（中消耗） | ~15min | 弹一小段钢琴、边缘学习一篇文章 | 有一点精力，想做点有产出感的事 |
| SSR（高消耗） | 30min+ | 跳舞、认真钻研一个小工具 | 精力充足，想沉浸进去 |

抽卡流程建议：**先选当前能量档位 → 再在该档位内随机**，避免抽到"需要 30 分钟但只剩 5 分钟电"的错配。

### 2.3 增强机制（后续可加）
- **彩蛋任务**：低概率"隐藏款"选项（如从没试过的新乐器 App、陌生领域科普），利用 novelty-driven 人格的稀有性钩子对抗审美疲劳。
- **代币解锁高档卡池**：专注工作（如番茄钟）攒的代币，可用于解锁更高档位/更稀有的抽卡权限——不是道德绑架式的"先工作才配娱乐"，而是游戏化的资源兑换关系。

### 2.4 落地形式
- 作为 `lab` repo 下的一个独立 project，网页形式实现。
- 每次「抽中并完成」，向账本数据库 insert 一条事件（`source: 抽卡`）。抽卡系统本身不存历史记录，历史全部交给账本。

---

## 三、代币账本（Ledger）

### 3.1 数据结构（核心一张事件表）

```
events
├── id
├── timestamp
├── source        # 抽卡 / 打卡 / 临时记录 / ...
├── activity       # 弹琴 / 边缘学习 / 英语课 / ...
├── duration        # 可选
├── tier / weight     # 可选，对应 R/SR/SSR 分档
└── note          # 可选，自由文本，记录当下感受
```

### 3.2 设计原则
- **只有一个写入口的语义**：不管数据来自抽卡、手动记录还是（未来可能的）外部同步，都统一成"事件"格式，避免多套记账逻辑互相打架。
- **不设消费出口**：不做"愿望清单兑换"——这是账本设计上最重要的一条边界，理由见上文（成年人的愿望清单已经失去激励效力）。
- **记账动作要比任何规则设计都轻**：宁可先把"发生了什么"原始地存下来，规则（如分档定价、成就阈值）可以后置、可以随时调整，不影响底层数据。

### 3.3 存储技术选型（留给你决定）
- 轻量本地方案：SQLite 文件，配合 `lab` 里的网页应用直接读写。
- 如果需要手机端也能写入（比如通勤时临时记一笔），需要一个能远程写入的方案（如轻量云数据库/自建小 API），复杂度会上升，需要权衡"极简"原则。

### 3.4 输出/视图（Ledger 的"出口"，非独立系统）
- **收集图鉴系统**（见下）
- **周期性回顾摘要**：按时间筛选 + GROUP BY，甚至可以直接把筛选结果丢给 AI 生成一段文字总结（"这段时间你其实做了这些"），对抗"好像什么都没留下"的空虚感。这是比游戏化奖励更耐久的抗倦怠燃料。

---

## 四、收集图鉴系统（Achievement / Collector）

### 4.1 定位
不是独立数据库，而是**基于规则的事件表扫描器**。规则可以用简单配置描述，例如：

```json
{
  "弹琴 10 次": "unlocked",
  "边缘学习 50 篇": "locked",
  "抽卡连续完成 7 天": "locked"
}
```

### 4.2 执行方式
- 定期（或实时）扫描 events 表中对应 activity 的 count / duration，达到阈值即点亮成就。
- 规则可以随时新增，不影响底层事件表结构——这是把「图鉴」和「账本」解耦的关键，保证图鉴系统的迭代成本极低。

---

## 五、三系统协作关系图（文字版）

```
[抽卡系统] ──完成后写入──▶ [账本 events 表] ◀──手动/临时记录写入──
                              │
                              ├──▶ [图鉴系统]（规则扫描，点亮成就）
                              └──▶ [周期回顾摘要]（GROUP BY + 可选 AI 总结）
```

- 抽卡是**随机触发器**，对付"不知道现在想干嘛但不想刷手机"的空窗期。
- 已有的三个外部打卡（多邻国/Fitness Boxing/微信读书）是**确定性触发器**，对付"知道该做但没冲动"的惯性任务——它们已经零摩擦，暂不强行并入本系统。
- 账本是唯一真相来源，图鉴和回顾摘要都是它的视图，不是平行系统。

---

## 六、尚待你决定的细节（有意留白，供实现时打磨）

1. **抽卡分档的具体阈值和活动清单**——R/SR/SSR 各自放哪些活动，你比任何人都清楚。
2. **数据库物理形态**——本地 SQLite vs. 云端可写方案，取决于你要不要手机端记录。
3. **三个外部打卡 App 要不要接入**——先确认多邻国/Fitness Boxing/微信读书是否有可自动化拉取的接口，再决定要不要做"元账本"同步，而不是手动搬运。
4. **图鉴规则的初始清单**——先用最少的几条规则跑起来，观察真实数据后再迭代，避免一开始就设计过度。
5. **事件表 `tier/weight` 字段是否要用于打卡任务**——目前三个外部打卡已经 0 摩擦，暂时不强制纳入账本，如果之后想让回顾摘要更完整，可以再补充这块。

---

*本规划书基于你已有的思考成果整理，具体实现细节和取舍交给你和 coding agent 继续打磨。*
